# Claude Code — local, unshared settings
.claude/settings.local.json

# macOS
.DS_Store

# Editors
.vscode/
.idea/
*.swp
*~

# Env
.env
.env.local

# Per-client Drive/Sheets delivery configs (carry Aleksandra's folder + sheet IDs).
# The .example schema reference is checked in; the populated file is not.
clients/*/delivery.yml

# Ruff
.ruff_cache/

# Claude Code agent worktrees (ephemeral, auto-created)
content-toolkit/worktrees/

__pycache__/

# format-to-wp secrets (ClickUp + WP credentials)
scripts/.env.format-to-wp
